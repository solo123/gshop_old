= simple_form_for @object, remote: :true,  html: {class: 'well form-horizontal'} do |f|
  = f.error_notification
  = f.input :brand
  = f.input :model
  = f.input :full_name
  = f.input :catalog
  = f.input :status
  .form-actions
    = f.submit "Save", :class => "btn btn-primary" 
