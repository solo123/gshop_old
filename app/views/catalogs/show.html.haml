%h2 Catalog!!!
.container-fluid
  .row-fluid
    .span2
      .catalog
        = @catalog.each do |cat|
          .row
            .col-md-2
              = link_to cat.catalog, catalog_path(cat: cat.catalog), remote: true
            .col-md-1
              = cat.cnt
    .span10

