<%= form_for([@parent, @photo], :html => { :multipart => true }) do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :photo_data_type %><br />
    <%= f.text_field :photo_data_type %>
  </div>
  <div class="field">
    <%= f.label :photo_data_id %><br />
    <%= f.number_field :photo_data_id %>
  </div>

  <%= f.file_field :pic %>

<% if @photo.pic %>
<%= image_tag @photo.pic.url(:medium) %>
<% end %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
